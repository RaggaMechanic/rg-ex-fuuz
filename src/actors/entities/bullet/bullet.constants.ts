import { Color, vec } from 'excalibur';

export const DEFAULT_BULLET_VELOCITY = 650;
export const DEFAULT_BULLET_DISTANCE = 300;
export const DEFAULT_BULLET_DAMAGE = 100;
export const DEFAULT_BULLET_TARGET_POS = vec(1, 0);
export const DEFAULT_BULLET_WIDTH = 5;
export const DEFAULT_BULLET_HEIGHT = 5;
export const DEFAULT_BULLET_COLOR = Color.Yellow;
